[pytest]
testpaths = tests
markers =
    unit: isolated unit test with no external dependencies
    integration: integration test exercising multiple modules
    e2e: end-to-end test hitting real services
